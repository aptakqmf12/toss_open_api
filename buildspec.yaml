version: 0.2
phases:
  install:
    commands:
      - n 20
      - node --version
  build:
    commands:
      - npm ci
      - npm run build
artifacts:
  files:
    - '**/*'
  exclude-paths:
    - .git/**